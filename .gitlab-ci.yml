image: node

build:
  stage: build
  before_script:
    - chmod +x ./env.sh
    - ./env.sh
  script:
    - npm install
    - npm run lint
    - npm run build
  environment:
    name: 'gatsby-build'
    url: 'localhost'
